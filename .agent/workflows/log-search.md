# /log-search

Search through and summarize session logs.

When this command is run:
1. Run the search/summarize script by executing: `python .sessions/scripts/search_logs.py`.
2. Review the output from the script.
3. Present a formatted, readable summary of the active logs and recent learnings to the user based on the script's output.
